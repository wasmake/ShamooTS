# Changesets

Run `pnpm changeset` for consumer-visible changes to packages under `packages/`. Commit the generated Markdown file with the change. Changesets are consumed when maintainers prepare a release.
