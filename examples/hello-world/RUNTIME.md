# Runtime deployment

Deploy this project to the ShamooRuntime Paper watched root, not Paper's general
`plugins` directory. The default watched root is
`<paper-server>/plugins/ShamooRuntime/plugins`.
