# @shamoo/platform-codegen

Shared validation, deterministic rendering, coverage, sync, and diff APIs for JVM scanner models. Models are versioned JSON artifacts; the generator never reflects against a locally installed server.
