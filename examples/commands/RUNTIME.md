# Runtime deployment

Deploy to the ShamooRuntime Paper watched root. The current adapter registers these
commands, but argument binding and guards require the full invocation adapter described
in the project README.
